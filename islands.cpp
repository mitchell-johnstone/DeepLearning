#include <bits.h>

int main(){
    int P;
    cin>>P;
    while(P--){
        int cur;
        cin>>cur;
        int nums[P];
        for(int i = 0; i < P; i++) cin>>nums[i];
        for(int l = 0; l < 10; l++){
            int min_e = nums[l+1];
            for(int r = 2; r < 12; r++){
            }
        }
        cout<<cur<<" ";
    }
    return 0;
}
